# Copy this file to .env and set the password before starting PostgreSQL.
# These values are applied only when the data volume is initialized.
# The real .env file is ignored by Git.

POSTGRES_DB=exchange_simulator
POSTGRES_USER=exchange_simulator
POSTGRES_PASSWORD=
POSTGRES_PORT=5432